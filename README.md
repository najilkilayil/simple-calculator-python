# Simple Calculator 
A simple and easy calculator using only Python.

Its my first python project. Iam trying to learn and work on more projects using python it just a beginning.

I added functions like addition, subtraction, division, multiplication, exponent and as well as square root. 

I changed the datatype of the resultant number according to the numbers (float into int).

Added a condition that in the divide function that a divisor value not must be 0.

Exponent with exponent model detail output print and short print also which is i done using for loop. 

Added square root without 'math' function and using while loop with condition "i*i = number" and also the condition which are not perfect sqare.

And finnaly a added all these in main function to work as a while true loop which is repeat the process as such as the user needed. And also added 'q' keyword to quit and break the program!

## To run
Can run the program simply running and downloading the file *main.py*.

## Future Plans
I would like to plan about  adding more functions such as largest amoung numbers, percentage, trigonometric functions, area and perimter of sqare, etc..